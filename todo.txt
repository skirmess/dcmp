
# create a function for _cwd
# 	_cwd { local $/; require Cwd; return cwd(); }  ???
# 	sprintf numberd arguments? $1 is cwd, $2 is error http://perldoc.perl.org/functions/sprintf.html
# 	mock cwd(), test if cwd() returns nothing
# move shared test code to t/lib module
# replace all for loops in tests with one iterator that returns all values?
# no encode for die!
# no decode for readdir, cwd, etc!
# replace all die, encode, decode, sort, cwd with DIE, ENCODE, DECODE, SORT, CWD!



#
# TODO rename left/right with 1/2 or reference/workspace or something
#   diff calls it file1/file2
#       %<     lines from FILE1
#       %>     lines from FILE2
#       %=     lines common to FILE1 and FILE2
#
